/* ============================================================
   VICE Launcher — Qt Style Sheet (QSS)
   Generated from the React design tokens.
   Use with PySide6 by loading one of the two themes at runtime:

       from PySide6.QtWidgets import QApplication
       from pathlib import Path

       app = QApplication([])
       qss = Path("themes/pixel.qss").read_text(encoding="utf-8")
       app.setStyleSheet(qss)

   Two files are provided:
       themes/pixel.qss  — A · Pixel Workshop (default)
       themes/c64.qss    — B · C64 Homage

   Object names the stylesheet targets (set via widget.setObjectName):
       #Sidebar, #SidebarItem, #SidebarItemActive
       #Toolbar, #SearchBox, #SearchInput
       #EmulatorCard, #EmulatorTitle, #EmulatorSub, #EmulatorExe
       #PrimaryButton, #SecondaryButton, #GhostButton
       #DropHint, #StatusPill
       #SettingsSidebar, #SettingsSection, #SettingsSectionTitle
       #InstallerSidebar, #InstallerStep, #InstallerStepActive
       #ProgressBar, #LogView
   ============================================================ */
